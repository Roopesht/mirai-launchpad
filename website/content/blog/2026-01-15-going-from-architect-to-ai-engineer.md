---
title: 'Going From Architect to AI Engineer'
date: '2026-01-15'
tags: ['career', 'ai', 'engineering']
excerpt: "Notes on what actually transfers from two decades of software architecture when you move into building AI-driven products — and what doesn't."
---

This is a placeholder post. Replace the frontmatter above and this body with your own writing.

## Why this post exists

Blog posts in this template are plain Markdown files with frontmatter, parsed at build time — no CMS, no database. To add your own post:

1. Create a new file under `content/blog/`, named `YYYY-MM-DD-your-slug.md`.
2. Fill in the frontmatter fields: `title`, `date`, `tags`, and `excerpt`.
3. Write the body in standard Markdown below the frontmatter.

## What to write about

A good first post is often something concrete: a lesson from a project, a tool you built, or a decision you had to make and why. Keep it in your own voice — the rest of the site carries the visual personality, so the writing itself can just be direct and useful.
